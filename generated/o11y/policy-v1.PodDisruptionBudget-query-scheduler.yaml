apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    name: query-scheduler
  name: query-scheduler
  namespace: o11y
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      name: query-scheduler
