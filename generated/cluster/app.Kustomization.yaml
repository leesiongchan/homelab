apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app
  namespace: app
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  interval: 1h
  path: ./generated/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  timeout: 3m
