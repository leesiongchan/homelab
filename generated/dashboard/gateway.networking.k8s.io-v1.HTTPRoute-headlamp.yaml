apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp
  namespace: dashboard
spec:
  hostnames:
  - kube-system
  parentRefs:
  - name: traefik-gateway
    namespace: network
  rules:
  - backendRefs:
    - name: headlamp
      namespace: dashboard
      port: 80
    matches:
    - path:
        value: /
