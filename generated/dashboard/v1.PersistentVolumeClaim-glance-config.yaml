apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: glance-config
  namespace: dashboard
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
