apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pocket-id
  namespace: auth
spec:
  chart:
    spec:
      chart: pocket-id
      sourceRef:
        apiVersion: 0.0.0
        kind: HelmRepository
        name: pocket-id-helm
  interval: 1h
  values: {}
